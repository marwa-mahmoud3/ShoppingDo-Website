@*@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("Logout", "Account", FormMethod.Post, new { id = "logout" }))
    {
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Hello,@User.Identity.Name</a>
        <div class="dropdown-menu">
            <a class="dropdown-item" href="javascript:document.getElementbyId('logout').submit()">Logout</a>
        </div>
    </li>
     }
}
        else
        {
    <li><a class="getstarted scrollto" href="/Account/Login">Sign in</a></li>
}*@
